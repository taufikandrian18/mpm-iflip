@model MPM.FLP.FLPDb.ProductCatalogs

<div class="row">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="header">
            <h5>Varian Harga</h5>
        </div>
        <div class="body">
            @using (Html.BeginForm("CreateVarianHarga", "ProductCatalog", FormMethod.Post))
            {
                <div class="form-group">
                    <input asp-for="Id" hidden />
                </div>
                <div class="form-group">
                    <label class="control-label"> Dokumen </label>
                    <br />
                    @(Html.Kendo().Upload()
                        .Name("files")
                        .Multiple(false)
                        .Validation(validation => validation.AllowedExtensions(new string[] { ".xlsx" }))
                    )
                </div>
                <div class="form-group">
                    <button type="submit" value="Create" class="btn btn-primary">Buat</button>
                </div>
            }
        </div>
    </div>
</div>
